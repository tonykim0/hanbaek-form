node_modules/
.next/
.env*
out/
.DS_Store
*.tsbuildinfo
.vercel

# Claude Code — 공유용 settings.json 은 커밋, 개인 설정은 제외
.claude/settings.local.json
~$*.docx

# 자료실 파일은 Vercel Blob에 올립니다 (git에 넣지 않음)
# — GitHub은 100MB 넘는 파일을 거부하고, 저장소가 무거워집니다.
# 로컬 폴더는 업로드 전 원본을 모아두는 용도로만 씁니다.
/public/materials/
