/**
 * 한백 자기 사업자 정보 — 거래명세서의 공급받는자 칸.
 *
 * ★왜 코드 상수인가★
 * 협력사 정보는 업체마다 달라 DB(partner_details)에 있지만, 이것은 한백 하나뿐이고
 * 바뀌는 일이 거의 없다. 그런 값을 화면과 표에 두면 「어디서 고치나」가 또 하나 생기고,
 * 정작 비어 있으면 명세서가 이름만 찍혀 나간다 — 실제로 그랬다(2026-08-24 리뷰).
 * 바뀌면 이 파일 한 줄을 고쳐 배포한다.
 *
 * 업태·종목은 두지 않는다 (한백 확인 2026-08-24).
 */
export const HANBAEK = {
  /** 상호 — 사업자등록증의 이름이 이것과 다르면 여기를 고친다 */
  name: '한백',
  /** 사업자등록번호 — 적힌 그대로 둔다(하이픈 포함). 협력사 쪽은 숫자만 저장해 화면에서 끼운다 */
  bizRegNo: '410-81-32391',
  ceo: '윤우람',
  addr: '광주광역시 광산구 비아로 23',
} as const;
