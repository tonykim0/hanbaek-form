-- 행위신고 불필요 — 그 현장은 신고 없이 시공으로 간다 (한백 지시 2026-08-26)
--
-- 「행위신고 완료」(notify_done_at) 하나로는 신고가 필요 없는 현장이 시공진행필요로 갈 길이
-- 없었다. 안 하는 일을 「했다」고 체크하게 두면 그 현장이 신고를 낸 것으로 남는다 —
-- 「없음」과 「해당없음」은 다른 값이다(화면 규칙 10). 그래서 칸을 따로 둔다.
--
-- 둘 중 하나면 「시공진행필요」의 조건이 찬다(lib/process.ts STATUS_GATES).
-- 날짜는 그렇게 판정한 날이다 — 체크의 기록 방식은 다른 완료 체크와 같다.
alter table processes add column if not exists notify_skipped_at text;
