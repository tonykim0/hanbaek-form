-- 행위신고 필요여부 — 「필요」로 정한 것도 남긴다 (한백 지시 2026-08-26)
--
-- 0024 가 「불필요」 칸을 만들었지만, 그 반대편은 담을 곳이 없었다. 그래서 「아직 안 정했다」와
-- 「필요하다고 정했다」가 같은 값(null)이었고, 화면은 신고일·파일 칸을 언제 열어야 하는지
-- 알 수 없어 늘 펴 두었다 — 안 낼 서류를 내라고 재촉하는 화면이 그것이다.
--
-- 필요여부를 먼저 고르고(필요·불필요), 고른 뒤에 그 아래가 열린다. 이 칸은 단계를 열지
-- 않는다 — 넘어가는 것은 완료(또는 불필요)다(lib/process.ts CHECK_ADVANCES).
alter table processes add column if not exists notify_required_at text;
