-- 「설치완료」 칸을 「개통 및 통신확인」으로 (한백 지시 2026-09-04)
--
-- 칸 이름이 상태였다. 설치가 끝났다는 것은 이 칸에 ★들어오는 조건★이고(설치완료일·
-- 설치완료 사진), 들어와서 하는 일은 통신·개통이다 — 이 칸이 담는 상자도 「개통」
-- 하나뿐이다(components/project/construction/milestones.tsx). 보드에 「설치완료」라는
-- 칸이 서면 거기 있는 현장이 무엇을 기다리는지 안 보인다.
--
-- ★지급 트리거 이름 「설치완료」는 그대로다★ (lib/settlement payoutReleaseOf) — 시공비
-- 1차를 여는 것은 「설치가 끝났다」는 사실이고, 그것은 칸이 아니다. 1차 영업비 트리거를
-- 「계약완료」 칸과 갈라 「계약서류 확인 완료」로 부른 것과 같은 이유다(2026-09-02).
-- 지급관리 화면의 「1차 · 설치완료 시 70%」는 바뀌지 않는다.
--
-- ★저장값을 반드시 옮긴다★ — asProcessStatus(lib/process)는 모르는 값을 첫 상태
-- (계약완료)로 되돌린다. 남겨 두면 그 현장이 공정 맨 앞으로 튕긴다(0048 이 같은 이유로
-- 옛 값을 옮겼다). 프로덕션 해당 현장 1건(확인 2026-09-04).

update processes set status = '개통 및 통신확인' where status = '설치완료';
